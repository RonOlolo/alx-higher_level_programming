#!/usr/bin/node
// Prints 3 lines but only an array of string and a loop

const threeLangs = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < 3; i++) {
  console.log(threeLangs[i]);
}
